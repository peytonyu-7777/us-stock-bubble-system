# Core
streamlit>=1.30
pandas>=2.2.0
numpy>=1.24
requests>=2.31
python-dotenv>=1.0       # load FRED_API_KEY (etc.) from a local .env file

# Data sources (all FREE / keyless except FRED)
yfinance>=0.2.40          # prices / momentum / QQQ-SPY  (Stooq fallback auto-used)
lxml>=4.9                 # yfinance / pandas_datareader XML/HTML parsing dep
pandas_datareader>=0.10   # FRED fallback fetcher (bounded by an internal timeout)
#   FRED primary path is the direct CSV endpoint over `requests` (no key needed).
#   Stooq + AAII also use `requests`. Every fetch is concurrent
#   (ThreadPoolExecutor max_workers=8) with a 5s per-request timeout.

# Parquet cache (pyarrow is the on-disk engine for the incremental cache)
pyarrow>=14.0

# Viz
plotly>=5.18

# Statistics — exact standard-normal CDF for the Z-score -> 0-100 mapping
# (pipeline.py imports this as scipy.stats.norm.cdf; a no-scipy erf fallback
# keeps the app running even if the install is skipped).
scipy>=1.10
